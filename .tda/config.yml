# TDA Project Configuration
# Generated by tda init project

project:
  name: "dynamodb-tools"

defaultWorkflow: standard

# Override agent settings (optional)
# agents:
#   defaultMaxTurns: 100

# Stages requiring approval (empty = use global)
# approvals:
#   requireApproval:
#     - specification
#     - design

# Commands for validators (detected by project analyzer)
# commands:
#   test: cargo test
#   lint: cargo clippy -- -D warnings
#   build: cargo build
